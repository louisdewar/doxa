/*
use async_trait::async_trait;

/// Each competition must create an executor that receives lines of input from each agent
/// participating in the match and determines actions.
///
/// The idea is that Executor abstracts over things such as WASM, stdin, stdout, rabbitmq, etc...
/// so that each competition needs to only focus on game logic.
pub trait CompetitionExecutor {
    fn setup(&mut self, agents: usize);
}
*/
